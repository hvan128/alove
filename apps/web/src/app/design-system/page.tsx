import { CheckCircleIcon, MicrophoneIcon, WarningCircleIcon } from '@phosphor-icons/react/dist/ssr'
import { AppShell } from '@/components/ui/app-shell'
import { Button } from '@/components/ui/button'
import { FieldEvidence } from '@/components/ui/field-evidence'
import { TextInput } from '@/components/ui/input'
import { Panel } from '@/components/ui/panel'
import { StatusDot, StatusPill } from '@/components/ui/status'

const colors = [
  ['Canvas', 'var(--canvas)', '#f5f5f7'], ['Pearl', 'var(--pearl)', '#fafafc'], ['Ink', 'var(--ink)', '#1d1d1f'], ['Action', 'var(--action)', '#0066cc'], ['Success', 'var(--success)', '#248a3d'], ['Warning', 'var(--warning)', '#b35c00'], ['Danger', 'var(--danger)', '#d70015'],
]

export default function DesignSystemPage() {
  return <AppShell><main className="mx-auto max-w-[1180px] px-4 py-10 sm:px-6"><header className="max-w-2xl"><p className="text-sm font-semibold text-[var(--action)]">OrderVoice foundations</p><h1 className="mt-2 text-4xl font-semibold tracking-[-0.05em]">Design system</h1><p className="mt-4 text-base leading-7 text-[var(--muted)]">Apple-like restraint for a dense operational product: one action blue, semantic exceptions, system typography, hairlines, no decorative gradients and no card shadows.</p></header><section className="mt-10"><h2 className="text-xl font-semibold tracking-[-0.03em]">Colour roles</h2><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{colors.map(([name, value, hex]) => <article className="rounded-[18px] border border-[var(--hairline)] bg-white p-3" key={name}><div className="h-20 rounded-xl border border-black/5" style={{ backgroundColor: value }} /><p className="mt-3 text-sm font-semibold">{name}</p><p className="mt-1 font-mono text-xs text-[var(--muted)]">{hex}</p></article>)}</div></section><section className="mt-10 grid gap-5 lg:grid-cols-2"><Panel title="Actions & state" eyebrow="Components"><div className="flex flex-wrap items-start gap-3"><Button>Primary action</Button><Button variant="secondary">Secondary</Button><Button variant="quiet">Quiet link</Button><Button disabled disabledReason="Cần chọn một đơn">Disabled</Button></div><div className="mt-6 flex flex-wrap gap-3"><StatusPill tone="success">Đã chốt</StatusPill><StatusPill tone="warning">Cần xử lý</StatusPill><StatusPill tone="demo">Demo cục bộ</StatusPill><StatusDot tone="info" label="Gateway đang kết nối" /></div></Panel><Panel title="Forms & evidence" eyebrow="Trust"><TextInput label="Tên khách hàng" placeholder="Cửa hàng Lan Anh" hint="Nhãn luôn liên kết input." /><FieldEvidence className="mt-5" quote="12 thùng cà phê Arabica" confidence={0.96} /></Panel></section><section className="mt-5 grid gap-5 lg:grid-cols-3"><Panel title="Type scale"><p className="text-[34px] font-semibold leading-none tracking-[-0.05em]">34px Display</p><p className="mt-4 text-[21px] font-semibold tracking-[-0.03em]">21px Section</p><p className="mt-4 text-[17px] leading-7">17px body copy helps operators read evidence without strain.</p><p className="mt-4 font-mono text-xs text-[var(--muted)]">12px · metrics / IDs / timestamps</p></Panel><Panel title="Status semantics"><div className="space-y-4"><div className="flex gap-2"><CheckCircleIcon className="text-[var(--success)]" size={20} weight="fill" /><p className="text-sm">Green: final, safe, resolved.</p></div><div className="flex gap-2"><WarningCircleIcon className="text-[var(--warning)]" size={20} weight="fill" /><p className="text-sm">Amber: review required, provider setup.</p></div><div className="flex gap-2"><MicrophoneIcon className="text-[var(--action)]" size={20} weight="fill" /><p className="text-sm">Blue: operator action and focus.</p></div></div></Panel><Panel title="Responsive rule"><p className="text-sm leading-6 text-[var(--muted)]">Three columns at ≥1180px; fluid stack below. Source choice and human approval remain high in the reading order. All core controls retain 44px hit targets.</p></Panel></section></main></AppShell>
}

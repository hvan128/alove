# Project Identity

- Canonical product and repository name: **Alove** (`alove`).
- Public GitHub repository: <https://github.com/hvan128/alove>.
- Vercel project: `alove`.
- Production URL: <https://vedi-one.vercel.app/>.
- Use `Alove`/`alove` in all new code, documentation, deployment metadata, agent names, and event topics. Do not reintroduce the former project name except when explicitly documenting migration history.
- Canonical runtime documentation is `README.md`, `docs/architecture.md`, `docs/deployment.md`, `specs/`, and ADR 0009. Dated files under `plans/` are historical working notes; never treat their old paths, fallbacks, URLs, or deployment claims as current behavior.

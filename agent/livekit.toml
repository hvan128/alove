# Placeholder only. Production uses the separately generated
# `livekit.production.toml` so this existing template cannot collide with the
# first Cloud create:
#   lk agent create --config livekit.production.toml ...
# The generated production file holds project/agent IDs but no secrets.
[project]
  subdomain = "your-project-subdomain"

[agent]
  id = ""
